import React from 'react';

function Listing({listing}) {

    return (

        <div>
            <div>
                {listing.map((item, index) => {
                    <div>

                        </div>
                })}
            </div>
        </div>
    )

}